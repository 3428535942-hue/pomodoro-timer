# 🎮 洛克王国·远行商人高价值商品监控推送系统 — 完整开发历程

> 项目时间：2026-04-30 至今（持续迭代中）

---

## 一、项目背景与目标

**目标**：监控《洛克王国》游戏中的限时商店"远行商人"，该 NPC 店铺每日分四个时段（08:00-12:00/12:00-16:00/16:00-20:00/20:00-24:00）刷新商品，部分高价值稀有商品限量上架，需要**第一时间获知并推送通知**，以便及时抢购。

**核心需求**：
- ⏰ 定时自动检测商品刷新
- 🔍 精准识别高价值稀有商品
- 📱 即时推送到微信，像好友消息一样醒目弹出
- 🤫 没有稀有商品时不打扰

---

## 二、项目架构总览

```
FC 定时触发器 (Cron: 08:05/12:05/16:05/20:05)
    ↓
fetchDailyShopItems() 爬取好游快爆页面
    ↓
正则解析 HTML，提取商品列表并按 4 个时段分类
    ↓
匹配高价值商品列表（棱镜球/祝福项坠/炫彩蛋/首领血脉秘药/国王球）
    ↓
发现高价值商品 → 构建 Markdown 精美排版消息
    ↓
获取企业微信 Token → POST 到企业微信 API
    ↓
管理员微信收到醒目推送 → 登录游戏购买
```

---

## 三、技术选型速查

| 模块 | 选用方案 | 原因 |
|------|---------|------|
| 运行环境 | Node.js v22+ | 轻量、自带 fetch |
| 部署平台 | 阿里云函数计算 FC | 免服务器、自动定时触发、零维护 |
| 定时调度 | FC 定时触发器（Cron） | 云原生，不需要 node-cron |
| 数据来源 | 好游快爆·远行商人查询器 | 实时更新，结构清晰 |
| 推送通道 | 企业微信应用消息 → 微信插件 | 像好友消息一样横幅+声音弹出 |
| 消息格式 | Markdown | 支持标题/加粗/引用/分割线等样式 |
| 监控看板 | schedule.html | 可视化查看各时段运行状态 |

---

## 四、完整开发历程

### 阶段一：原始项目搭建（Server酱方案）

#### 4.1 项目初始化

项目最初采用标准 Node.js 工程结构：

```
project/
├── package.json           — Node.js 配置（ES Module）
├── .env                   — Server酱 API 密钥
├── data/config.json       — 监控目标配置
└── src/
    ├── index.js           — 主入口 + node-cron 定时调度
    ├── scraper.js         — 数据爬取模块
    ├── check.js           — 高价值商品比对逻辑
    └── notifier.js        — Server酱推送模块
```

#### 4.2 数据源探索（最关键的阶段）

先后尝试了 3 个数据源：

| 数据源 | 结果 | 原因 |
|-------|:----:|------|
| ❌ 小红书博主主页 | 失败 | 反爬严格，内容非结构化 |
| ❌ 游侠网攻略页面 | 失败 | 手动更新，数据严重滞后 |
| ✅ **好游快爆查询器** | **成功** | **实时更新，数据结构清晰** |

好游快爆提供一个远行商人实时查询页面（HTML），通过正则表达式提取商品名称、价格（洛克贝）、限购数量等信息。

#### 4.3 定时调度

每天 4 个时段，刷新后 1 分钟触发检测：

| 时段 | 原检测时间 | 后改为 |
|------|:---------:|:------:|
| 第一轮 08:00-12:00 | 08:01 | **08:05** |
| 第二轮 12:00-16:00 | 12:01 | **12:05** |
| 第三轮 16:00-20:00 | 16:01 | **16:05** |
| 第四轮 20:00-24:00 | 20:01 | **20:05** |

> **为什么改时间？** 数据源（好游快爆查询器）存在短暂的缓存延迟，商品刷新后不会立即更新，改为 05 分检测确保数据已稳定。

#### 4.4 遇到的问题与解决（原始阶段）

| 问题 | 解决方案 |
|------|---------|
| 个人公众号 API 不能自动群发 | 改用 Server酱 通知管理员 → 手动群发 |
| 攻略站数据滞后不准确 | 换用好游快爆实时查询器 |
| HTML 正则匹配困难 | 先保存页面文件，逐步调试正则 |
| 终端引号转义报错 | 复杂代码写入 .js 文件再执行 |

---

### 阶段二：12:01 推送失败排查

#### 问题现象

部署到阿里云 FC 后，12:01 的定时检测没有推送消息到微信。

#### 根因分析

**阿里云 FC 的默认时区是 UTC（格林威治标准时间），而不是北京时间（UTC+8）。**

代码中使用 `new Date().getHours()` 获取当前小时，在 FC 上返回的是 UTC 时间：

| 北京时间 | FC 服务器时间 | getHours() | 判断结果 |
|---------|-------------|-----------|---------|
| 08:01 | 00:01 UTC | h=0 | ❌ 商店打烊 |
| **12:01** | **04:01 UTC** | **h=4** | **❌ 商店打烊** |
| 16:01 | 08:01 UTC | h=8 | ✅ 但算成第一时段 |
| 20:01 | 12:01 UTC | h=12 | ✅ 但算成第二时段 |

**修复方案**：创建北京时间工具函数

```javascript
function getBeijingHour() {
  const utcHours = new Date().getUTCHours();
  let beijingHours = utcHours + 8;
  if (beijingHours >= 24) beijingHours -= 24;
  return beijingHours;
}

function getBeijingTime() {
  const now = new Date();
  // ... 通过 getUTC*() 方法 + 8 小时偏移拼接北京时间
}
```

**经验教训**：云函数场景下时区问题必须显式处理，不能依赖服务器本地时间。

---

### 阶段三：语法错误修复

#### 问题现象

FC 部署时报错：`exports is not defined in ES module scope`

#### 根因分析

FC Node.js 20 运行环境将代码作为 **ES Module（.mjs）** 执行，但代码使用了 CommonJS 的 `exports.handler` 语法。ES Module 作用域中没有 `exports` 对象。

#### 修复方案

```javascript
// ❌ 错误（CommonJS）
exports.handler = async (event, context, callback) => { ... };

// ✅ 正确（ES Module）
export const handler = async (event, context, callback) => { ... };
```

---

### 阶段四：高价值商品列表调整

#### 调整内容

| 操作 | 原值 | 改为 |
|------|------|------|
| 🔄 修改 | 炫彩精灵蛋 | **炫彩蛋** |
| 🆕 新增 | — | **首领血脉秘药** |
| 🆕 新增 | — | **国王球** |
| ✅ 保留 | 棱镜球 | 棱镜球 |
| ✅ 保留 | 祝福项坠 | 祝福项坠 |

#### 最终监控列表

```javascript
const rareNames = ["棱镜球", "祝福项坠", "炫彩蛋", "首领血脉秘药", "国王球"];
```

匹配逻辑采用**模糊包含匹配**（`i.name.includes(n)`），避免游戏内名称前后缀变化导致的漏匹配。

---

### 阶段五：推送排版大改版

#### 改版前后对比

**改版前（纯文本）**：
```
发现棱镜球！请群发公众号
远行商人刷新高价值商品！
检测时间: 2026-05-09 12:05
━━━ 12:00:00-16:00:00 商品列表 ━━━
• 棱鱼 | 💰48,800洛克贝 | 限购3 高价值
```

**改版后（Markdown 精美排版）**：
```markdown
# 🔥 手慢无！远行商人稀有商品限时上架！

**检测时间：** 2026-05-09 12:05　**当前时段：** 12:00-16:00

---

## ⭐ 本店稀有

> 1. **棱镜球**　💰48,800洛克贝　限购3
> 2. **国王球**　💰5,000洛克贝

---

本时段共上架 48 件商品

> 🎮 打开游戏地图 → 点击火苗图标 → 传送至远行商人
```

#### 设计要点

| 元素 | 用途 |
|------|------|
| `# 主标题` | 大号醒目标题，🔥 表情增加紧迫感 |
| `---` 分割线 | 区分信息区块 |
| `**加粗**` | 突出稀有商品名称 |
| `> 引用块` | 展示商品详情，视觉聚焦 |
| 全角空格 `　` | 字段间对齐 |
| 底部指引 | 引导用户进游戏购买 |

---

### 阶段六：去除无稀有推送

#### 问题

原始版本每次检测无论是否发现稀有商品都会推送，导致大量无效消息干扰用户。

#### 修复

```javascript
if (found.length > 0) {
  // 发现稀有 → 推送精美排版
  await sendPush(md);
}
// 未发现稀有 → 静默，不打扰
```

只推送有价值的信息，避免通知疲劳。

---

### 阶段七：清理无用代码

随着项目迭代，清理了以下内容：

- ❌ 移除 Server酱 API 调用代码和相关配置
- ❌ 移除 node-cron 本地定时调度（改为 FC 内置触发器）
- ❌ 移除商品对象的 `image` 字段（推送中不再使用图片）
- ❌ 移除独立的模块文件，全部内聚到 FC 单一入口文件

---

### 阶段八：接入企业微信 — 踩坑全记录

这是整个项目踩坑最多、最复杂的环节。

#### 8.1 为什么切换到企业微信？

| 方案 | 弹出效果 | 缺点 |
|------|---------|------|
| Server酱 | 在"服务通知"中，易被折叠 | 不够醒目 |
| **企业微信应用消息** | **像好友消息一样横幅+声音+亮屏** | **配置复杂** |
| 个人微信机器人 | 最好 | 极易封号 |

为了"像微信好友发消息一样醒目弹出"，决定从 Server酱 切换到企业微信。

#### 8.2 坑①：IP 白名单

**错误提示**：
```
企业微信推送失败: not allow to access from your ip, hint: [...], from ip: 47.97.249.71
```

**原因**：企业微信 API 要求调用来源 IP 必须在应用的"企业可信IP"白名单内。

**解决尝试**：
- 在应用配置中找到了"企业可信IP"设置项
- ⚠️ 但该功能要求**先设置可信域名或接收消息服务器URL**

#### 8.3 坑②：可信域名

**提示**：`配置企业可信IP前，请先设置可信域名或设置接收消息服务器URL`

**尝试方案 A — 接收消息服务器URL**：
- 在 FC 创建 HTTP 触发器，获得公网 URL
- 填入企业微信 → ❌ 提示"该域名主体为第三方服务商"

**尝试方案 B — 可信域名**：
- 购买了域名 `panyuan.cyou`
- ⚠️ 要求域名必须完成 ICP 备案
- 阿里云备案需要购买 ECS 服务器（最低 24元/月 × 3个月）
- 备案审核需要 **7~20 天**

#### 8.4 坑③：IP 访问限制是付费功能

在"安全与管理 → IP 访问限制"中看到的是**邮件/微盘的访问控制**，与企业微信 API 调用无关，且需要付费会员。

#### 8.5 尝试方案 C — Cloudflare + 域名指向 FC

**思路**：用 Cloudflare 给域名 `shop.panyuan.cyou` 做反向代理到 FC，由 Cloudflare 提供合法 SSL 证书。

**进展**：
- DNS 成功解析到 FC ✅
- 但 SSL 证书不匹配（FC 的证书是发给 `*.fcapp.run` 的）
- 浏览器提示 `NET::ERR_CERT_COMMON_NAME_INVALID`

**后续**：注册 Cloudflare，配置代理模式和 SSL，让企业微信能成功验证 URL。

#### 8.6 尝试方案 D — 群机器人

**思路**：企业微信群的"群机器人"功能不需要配任何 IP 白名单。

**问题**：用户的企业微信 App 版本界面没有"群机器人"选项，只有"智能机器人"（AI助手）。

---

## 五、最终项目文件

### 文件清单

| 文件 | 路径 | 说明 |
|------|------|------|
| ⚙️ fc-code.txt | [fc-code.txt](file:///e:/Program%20Files%20(x86)/trae/trae/fc-code.txt) | FC 云函数核心代码（爬虫+比对+企业微信推送） |
| 📊 schedule.html | [schedule.html](file:///e:/Program%20Files%20(x86)/trae/trae/schedule.html) | 可视化监控看板（4 时段状态实时显示） |
| 📝 SUMMARY.md | [SUMMARY.md](file:///e:/Program%20Files%20(x86)/trae/trae/SUMMARY.md) | 开发流程总结文档 |
| 🧪 代码1.mjs | `D:\工作文件家\远行商人测试文件\代码1.mjs` | 本地测试文件 |
| 🔑 .env | `D:\工作文件家\远行商人测试文件\.env` | 企业微信凭证（本地测试用） |

### FC 环境变量配置

| 变量名 | 说明 |
|-------|------|
| `WX_CORPID` | 企业微信企业ID |
| `WX_AGENTID` | 应用ID |
| `WX_SECRET` | 应用Secret密钥 |

---

## 六、踩坑排行榜

| 排名 | 问题 | 耗时 | 严重程度 |
|:----:|------|:----:|:--------:|
| 🥇 | **企业微信 IP 白名单** | 最长 | 致命（推送完全不可用） |
| 🥈 | **FC 时区问题（UTC vs 北京时间）** | 较长 | 高（时段判断全部错误） |
| 🥉 | **企业微信可信域名前置条件** | 较长 | 高（路径被卡死） |
| 4 | Server酱 → 企业微信迁移 | 中等 | 中（重构工作量大） |
| 5 | ESM 语法错误（exports vs export） | 短 | 高（函数无法部署） |
| 6 | 数据源正则表达式调试 | 中等 | 中（爬虫逻辑核心） |
| 7 | Cloudflare Workers 方案尝试 | 中等 | 低（未采用） |

---

## 七、经验总结

### 7.1 关键成功要素

1. **数据源选型最重要** — 好游快爆实时查询器是项目成功的基础
2. **云函数时区必须显式处理** — UTC 与北京时间差 8 小时
3. **云函数环境差异需充分考虑** — FC 与本地在模块规范、API 支持等方面存在差异
4. **企业微信的坑主要在配置层面** — IP 白名单 + 可信域名是最大拦路虎
5. **推送频率控制很重要** — 只推有价值的信息，避免通知疲劳
6. **不理解的东西直接问 AI** — 减少盲目试错

### 7.2 快速复用指南

任何需要监控 + 推送的场景都可以复用这套模式：

1. 确定数据源（找实时 API 或页面）
2. 编写爬虫提取数据（`fetchDailyShopItems`）
3. 定义监控目标（`highValueNames` 列表）
4. 配置推送通道（企业微信/Server酱）
5. 部署到云函数（FC/SCF）并配好定时触发器
6. 推送排版优化（Markdown 美化）

---

## 八、待办事项

- [ ] **Cloudflare 配置完成** — 注册 Cloudflare，配置域名代理和 SSL
- [ ] **企业微信验证通过** — 配好接收消息服务器URL
- [ ] **配置企业可信 IP** — 填入 FC 出口 IP `47.97.249.71`
- [ ] **正式上线运行** — 企业微信推送跑通
- [ ] **可选：购买 ECS 服务器** — 完成 ICP 备案（如需长期稳定方案）

---

> 项目状态：**开发完成，待企业微信验证上线**
> 最后更新：2026-05-02
